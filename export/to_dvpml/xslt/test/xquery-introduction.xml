<?xml version="1.0" encoding="UTF-8"?>
<document>
   <entete>
      <rubrique>65</rubrique>
      <meta>
         <description/>
         <keywords>documentation, qt, français</keywords>
      </meta>
      <titre>
         <page>A Short Path to XQuery</page>
         <article>A Short Path to XQuery</article>
      </titre>
      <date>2018-09-12</date>
      <miseajour>2018-09-12</miseajour>
      <includebas>include($_SERVER['DOCUMENT_ROOT'] . '/doc/pied.php');include($_SERVER['DOCUMENT_ROOT'] . '/template/pied.php');</includebas>
      <serveur>Qt</serveur>
      <chemin>/doc/qt/1.2/xquery-introduction</chemin>
      <urlhttp>http://qt.developpez.com/doc/qt/1.2/xquery-introduction</urlhttp>
   </entete>
   <voiraussi>
      <lien>
         <texte>XQuery</texte>
         <url/>
      </lien>
   </voiraussi>
   <authorDescriptions>
      <authorDescription name="Dummy" role="auteur">
         <fullname>Dummy</fullname>
         <url>https://www.developpez.net/forums/u1/dummy/</url>
      </authorDescription>
   </authorDescriptions>
   <synopsis>
      <paragraph type="important" sautDePagePdf="0"/>
   </synopsis>
   <summary>
      <section id="1" noNumber="0" sautDePagePdf="0">
         <title>Introduction</title>
         <paragraph type="important" sautDePagePdf="0">Where Java and C++ are <i>statement-based</i> languages, the <link href="xmlprocessing.html">XQuery</link> language is <i>expression-based</i>. The simplest <link href="xmlprocessing.html">XQuery</link> expression is an XML element constructor:</paragraph>
         <code langage="other">
&lt;recipe/&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">This <inline>&lt;recipe/&gt;</inline> element is an <link href="xmlprocessing.html">XQuery</link> expression that forms a complete <link href="xmlprocessing.html">XQuery</link>. In fact, this <link href="xmlprocessing.html">XQuery</link> doesn't actually query anything. It just creates an empty <inline>&lt;recipe/&gt;</inline> element in the output. But <link href="xquery-introduction.html#constructing-elements">constructing new elements in an XQuery</link> is often necessary.</paragraph>
         <paragraph type="important" sautDePagePdf="0">An <link href="xmlprocessing.html">XQuery</link> expression can also be enclosed in curly braces and embedded in another <link href="xmlprocessing.html">XQuery</link> expression. This <link href="xmlprocessing.html">XQuery</link> has a document expression embedded in a node expression:</paragraph>
         <code langage="other">
 &lt;html xmlns="http://www.w3.org/1999/xhtml/"
       xml:id="{doc("other.html")/html/@xml:id}"/&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">It creates a new <inline>&lt;html&gt;</inline> element in the output and sets its <inline>id</inline> attribute to be the <inline>id</inline> attribute from an <inline>&lt;html&gt;</inline> element in the <inline>other.html</inline> file.</paragraph>
      </section>
      <section id="2" noNumber="0" sautDePagePdf="0">
         <title>Using Path Expressions To Match And Select Items</title>
         <paragraph type="important" sautDePagePdf="0">In C++ and Java, we write nested <inline>for</inline> loops and recursive functions to traverse XML trees in search of elements of interest. In <link href="xmlprocessing.html">XQuery</link>, we write these iterative and recursive algorithms with <i>path expressions</i>.</paragraph>
         <paragraph type="important" sautDePagePdf="0">A path expression looks somewhat like a typical <i>file pathname</i> for locating a file in a hierarchical file system. It is a sequence of one or more <i>steps</i> separated by slash '/' or double slash '//'. Although path expressions are used for traversing XML trees, not file systems, in Qt XML Patterns we can model a file system to look like an XML tree, so in Qt XML Patterns we can use <link href="xmlprocessing.html">XQuery</link> to traverse a file system. See the <link href="qtxmlpatterns-filetree-example.html">file system example</link>.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Think of a path expression as an algorithm for traversing an XML tree to find and collect items of interest. This algorithm is evaluated by evaluating each step moving from left to right through the sequence. A step is evaluated with a set of input items (nodes and atomic values), sometimes called the <i>focus</i>. The step is evaluated for each item in the focus. These evaluations produce a new set of items, called the <i>result</i>, which then becomes the focus that is passed to the next step. Evaluation of the final step produces the final result, which is the result of the <link href="xmlprocessing.html">XQuery</link>. The items in the result set are presented in document order and without duplicates.</paragraph>
         <paragraph type="important" sautDePagePdf="0">With Qt XML Patterns, a standard way to present the initial focus to a query is to call <inline><link href="qxmlquery.html#setFocus">QXmlQuery::setFocus()</link></inline>. Another common way is to let the <link href="xmlprocessing.html">XQuery</link> itself create the initial focus by using the first step of the path expression to call the <link href="xmlprocessing.html">XQuery</link>
            <inline>doc()</inline> function. The <inline>doc()</inline> function loads an XML document and returns the <i>document node</i>. Note that the document node is <i>not</i> the same as the <i>document element</i>. The <i>document node</i> is a node constructed in memory, when the document is loaded. It represents the entire XML document, not the document element. The <i>document element</i> is the single, top-level XML element in the file. The <inline>doc()</inline> function returns the document node, which becomes the singleton node in the initial focus set. The document node will have one child node, and that child node will represent the document element. Consider the following <link href="xmlprocessing.html">XQuery</link>:</paragraph>
         <code langage="other">
doc('cookbook.xml')//recipe
</code>
         <paragraph type="important" sautDePagePdf="0">The <inline>doc()</inline> function loads the <inline>cookbook.xml</inline> file and returns the document node. The document node then becomes the focus for the next step <inline>//recipe</inline>. Here the double slash means select all <inline>&lt;recipe&gt;</inline> elements found below the document node, regardless of where they appear in the document tree. The query selects all <inline>&lt;recipe&gt;</inline> elements in the cookbook. See <link href="xquery-introduction.html#running-the-cookbook-examples">Running The Cookbook Examples</link> for instructions on how to run this query (and most of the ones that follow) from the command line.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Conceptually, evaluation of the steps of a path expression is similar to iterating through the same number of nested <i>for</i> loops. Consider the following <link href="xmlprocessing.html">XQuery</link>, which builds on the previous one:</paragraph>
         <code langage="other">
doc('cookbook.xml')//recipe/title
</code>
         <paragraph type="important" sautDePagePdf="0">This <link href="xmlprocessing.html">XQuery</link> is a single path expression composed of three steps. The first step creates the initial focus by calling the <inline>doc()</inline> function. We can paraphrase what the query engine does at each step:</paragraph>
         <liste type="1" sautDePagePdf="0">
            <element useText="0"/>
            <element useText="0"/>
            <element useText="0"/>
         </liste>
         <paragraph type="important" sautDePagePdf="0">Again the double slash means select all the <inline>&lt;recipe&gt;</inline> elements in the document. The single slash before the <inline>&lt;title&gt;</inline> element means select only those <inline>&lt;title&gt;</inline> elements that are <i>child</i> elements of a <inline>&lt;recipe&gt;</inline> element (i.e. not grandchildren, etc). The <link href="xmlprocessing.html">XQuery</link> evaluates to a final result set containing the <inline>&lt;title&gt;</inline> element of each <inline>&lt;recipe&gt;</inline> element in the cookbook.</paragraph>
      </section>
      <section id="3" noNumber="0" sautDePagePdf="0">
         <title>Axis Steps</title>
         <paragraph type="important" sautDePagePdf="0">The most common kind of path step is called an <i>axis step</i>, which tells the query engine which way to navigate from the context node, and which test to perform when it encounters nodes along the way. An axis step has two parts, an <i>axis specifier</i>, and a <i>node test</i>. Conceptually, evaluation of an axis step proceeds as follows: For each node in the focus set, the query engine navigates out from the node along the specified axis and applies the node test to each node it encounters. The nodes selected by the node test are collected in the result set, which becomes the focus set for the next step.</paragraph>
         <paragraph type="important" sautDePagePdf="0">In the example <link href="xmlprocessing.html">XQuery</link> above, the second and third steps are both axis steps. Both apply the <inline>element(name)</inline> node test to nodes encountered while traversing along some axis. But in this example, the two axis steps are written in a <link href="xquery-introduction.html#shorthand-form">shorthand form</link>, where the axis specifier and the node test are not written explicitly but are implied. XQueries are normally written in this shorthand form, but they can also be written in the longhand form. If we rewrite the <link href="xmlprocessing.html">XQuery</link> in the longhand form, it looks like this:</paragraph>
         <code langage="other">
doc('cookbook.xml')/descendant-or-self::element(recipe)/child::element(title)
</code>
         <paragraph type="important" sautDePagePdf="0">The two axis steps have been expanded. The first step (<inline>//recipe</inline>) has been rewritten as <inline>/descendant-or-self::element(recipe)</inline>, where <inline>descendant-or-self::</inline> is the axis specifier and <inline>element(recipe)</inline> is the node test. The second step (<inline>title</inline>) has been rewritten as <inline>/child::element(title)</inline>, where <inline>child::</inline> is the axis specifier and <inline>element(title)</inline> is the node test. The output of the expanded <link href="xmlprocessing.html">XQuery</link> will be exactly the same as the output of the shorthand form.</paragraph>
         <paragraph type="important" sautDePagePdf="0">To create an axis step, concatenate an axis specifier and a node test. The following sections list the axis specifiers and node tests that are available.</paragraph>
      </section>
      <section id="4" noNumber="0" sautDePagePdf="0">
         <title>Axis Specifiers</title>
         <paragraph type="important" sautDePagePdf="0">An axis specifier defines the direction you want the query engine to take, when it navigates away from the context node. Qt XML Patterns supports the following axes.</paragraph>
         <tableau width="80%" border="1" sautDePagePdf="0">
            <entete>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </entete>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
         </tableau>
      </section>
      <section id="5" noNumber="0" sautDePagePdf="0">
         <title>Node Tests</title>
         <paragraph type="important" sautDePagePdf="0">A node test is a conditional expression that must be true for a node if the node is to be selected by the axis step. The conditional expression can test just the <i>kind</i> of node, or it can test the <i>kind</i> of node and the <i>name</i> of the node. The <link href="xmlprocessing.html">XQuery</link> specification for node tests also defines a third condition, the node's <i>Schema Type</i>, but schema type tests are not supported in Qt XML Patterns.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Qt XML Patterns supports the following node tests. The tests that have a <inline>name</inline> parameter test the node's name in addition to its <i>kind</i> and are often called the <link href="xquery-introduction.html#name-tests">Name Tests</link>.</paragraph>
         <tableau width="80%" border="1" sautDePagePdf="0">
            <entete>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </entete>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
         </tableau>
      </section>
      <section id="6" noNumber="0" sautDePagePdf="0">
         <title>Shorthand Form</title>
         <paragraph type="important" sautDePagePdf="0">Writing axis steps using the longhand form with axis specifiers and node tests is semantically clear but syntactically verbose. The shorthand form is easy to learn and, once you learn it, just as easy to read. In the shorthand form, the axis specifier and node test are implied by the syntax. XQueries are normally written in the shorthand form. Here is a table of some frequently used shorthand forms:</paragraph>
         <tableau width="80%" border="1" sautDePagePdf="0">
            <entete>
               <colonne useText="0"/>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </entete>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
         </tableau>
         <paragraph type="important" sautDePagePdf="0">The <link href="xmlprocessing.html">XQuery</link> language specification has a more detailed section on the shorthand form, which it calls the abbreviated syntax. More examples of path expressions written in the shorthand form are found there. There is also a section listing examples of path expressions written in the longhand form.</paragraph>
      </section>
      <section id="7" noNumber="0" sautDePagePdf="0">
         <title>Name Tests</title>
         <paragraph type="important" sautDePagePdf="0">The name tests are the <link href="xquery-introduction.html#node-tests">Node Tests</link> that have the <inline>name</inline> parameter. A name test must match the node <i>name</i> in addition to the node <i>kind</i>. We have already seen name tests used:</paragraph>
         <code langage="other">
doc('cookbook.xml')//recipe/title
</code>
         <paragraph type="important" sautDePagePdf="0">In this path expression, both <inline>recipe</inline> and <inline>title</inline> are name tests written in the shorthand form. <link href="xmlprocessing.html">XQuery</link> resolves these names (QNames) to their expanded form using whatever namespace declarations it knows about. Resolving a name to its expanded form means replacing its namespace prefix, if one is present (there aren't any present in the example), with a namespace URI. The expanded name then consists of the namespace URI and the local name.</paragraph>
         <paragraph type="important" sautDePagePdf="0">But the names in the example above don't have namespace prefixes, because we didn't include a namespace declaration in our <inline>cookbook.xml</inline> file. However, we will often use <link href="xmlprocessing.html">XQuery</link> to query XML documents that use namespaces. Forgetting to declare the correct namespace(s) in an <link href="xmlprocessing.html">XQuery</link> is a common cause of <link href="xmlprocessing.html">XQuery</link> failures. Let's add a <i>default</i> namespace to <inline>cookbook.xml</inline> now. Change the <i>document element</i> in <inline>cookbook.xml</inline> from:</paragraph>
         <code langage="other">
&lt;cookbook&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">to...</paragraph>
         <code langage="other">
&lt;cookbook xmlns="http://cookbook/namespace"&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">This is called a <i>default namespace</i> declaration because it doesn't include a namespace prefix. By including this default namespace declaration in the document element, we mean that all unprefixed <i>element</i> names in the document, including the document element itself (<inline>cookbook</inline>), are automatically in the default namespace <inline>http://cookbook/namespace</inline>. Note that unprefixed <i>attribute</i> names are not affected by the default namespace declaration. They are always considered to be in <i>no namespace</i>. Note also that the URL we choose as our namespace URI need not refer to an actual location, and doesn't refer to one in this case. But click on http://www.w3.org/XML/1998/namespace, for example, which is the namespace URI for elements and attributes prefixed with <inline>xml:</inline>.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Now when we try to run the previous <link href="xmlprocessing.html">XQuery</link> example, no output is produced! The path expression no longer matches anything in the cookbook file because our <link href="xmlprocessing.html">XQuery</link> doesn't yet know about the namespace declaration we added to the cookbook document. There are two ways we can declare the namespace in the <link href="xmlprocessing.html">XQuery</link>. We can give it a <i>namespace prefix</i> (e.g. <inline>c</inline> for cookbook) and prefix each name test with the namespace prefix:</paragraph>
         <code langage="other">
declare namespace c = "http://cookbook/namespace";
doc('cookbook.xml')//c:recipe/c:title
</code>
         <paragraph type="important" sautDePagePdf="0">Or we can declare the namespace to be the <i>default element namespace</i>, and then we can still run the original <link href="xmlprocessing.html">XQuery</link>:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')//recipe/title
</code>
         <paragraph type="important" sautDePagePdf="0">Both methods will work and produce the same output, all the <inline>&lt;title&gt;</inline> elements:</paragraph>
         <code langage="other">
&lt;title xmlns="http://cookbook/namespace"&gt;Quick and Easy Mushroom Soup&lt;/title&gt;
&lt;title xmlns="http://cookbook/namespace"&gt;Cheese on Toast&lt;/title&gt;
&lt;title xmlns="http://cookbook/namespace"&gt;Hard-Boiled Eggs&lt;/title&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">But note how the output is slightly different from the output we saw before we added the default namespace declaration to the cookbook file. Qt XML Patterns automatically includes the correct namespace attribute in each <inline>&lt;title&gt;</inline> element in the output. When Qt XML Patterns loads a document and expands a QName, it creates an instance of <inline><link href="qxmlname.html">QXmlName</link></inline>, which retains the namespace prefix along with the namespace URI and the local name. See <inline><link href="qxmlname.html">QXmlName</link></inline> for further details.</paragraph>
         <paragraph type="important" sautDePagePdf="0">One thing to keep in mind from this namespace discussion, whether you run XQueries in a Qt program using Qt XML Patterns, or you run them from the command line using xmlpatterns, is that if you don't get the output you expect, it might be because the data you are querying uses namespaces, but you didn't declare those namespaces in your <link href="xmlprocessing.html">XQuery</link>.</paragraph>
      </section>
      <section id="8" noNumber="0" sautDePagePdf="0">
         <title>Wildcards in Name Tests</title>
         <paragraph type="important" sautDePagePdf="0">The wildcard <inline>'*'</inline> can be used in a name test. To find all the attributes in the cookbook but select only the ones in the <inline>xml</inline> namespace, use the <inline>xml:</inline> namespace prefix but replace the <i>local name</i> (the attribute name) with the wildcard:</paragraph>
         <code langage="other">
doc('cookbook.xml')//@xml:*
</code>
         <paragraph type="important" sautDePagePdf="0">Oops! If you save this <link href="xmlprocessing.html">XQuery</link> in <inline>file.xq</inline> and run it through <inline>xmlpatterns</inline>, it doesn't work. You get an error message instead, something like this: <i>Error SENR0001 in file:///...file.xq, at line 1, column 1: Attribute xml:id can't be serialized because it appears at the top level.</i> The <link href="xmlprocessing.html">XQuery</link> actually ran correctly. It selected a bunch of <inline>xml:id</inline> attributes and put them in the result set. But then <inline>xmlpatterns</inline> sent the result set to a <inline><link href="qxmlserializer.html">serializer</link></inline>, which tried to output it as well-formed XML. Since the result set contains only attributes and attributes alone are not well-formed XML, the <inline><link href="qxmlserializer.html">serializer</link></inline> reports a serialization error.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Fear not. <link href="xmlprocessing.html">XQuery</link> can do more than just find and select elements and attributes. It can <link href="xquery-introduction.html#constructing-elements">construct new ones on the fly</link> as well, which is what we need to do here if we want <inline>xmlpatterns</inline> to let us see the attributes we selected. The example above and the ones below are revisited in the <link href="xquery-introduction.html#constructing-elements">Constructing Elements</link> section. You can jump ahead to see the modified examples now, and then come back, or you can press on from here.</paragraph>
         <paragraph type="important" sautDePagePdf="0">To find all the <inline>name</inline> attributes in the cookbook and select them all regardless of their namespace, replace the namespace prefix with the wildcard and write <inline>name</inline> (the attribute name) as the local name:</paragraph>
         <code langage="other">
doc('cookbook.xml')//@*:name
</code>
         <paragraph type="important" sautDePagePdf="0">To find and select all the attributes of the <i>document element</i> in the cookbook, replace the entire name test with the wildcard:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/@*
</code>
      </section>
      <section id="9" noNumber="0" sautDePagePdf="0">
         <title>Using Predicates In Path Expressions</title>
         <paragraph type="important" sautDePagePdf="0">Predicates can be used to further filter the nodes selected by a path expression. A predicate is an expression in square brackets ('[' and ']') that either returns a boolean value or a number. A predicate can appear at the end of any path step in a path expression. The predicate is applied to each node in the focus set. If a node passes the filter, the node is included in the result set. The query below selects the recipe element that has the <inline>&lt;title&gt;</inline> element <inline>"Hard-Boiled Eggs"</inline>.</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc("cookbook.xml")/cookbook/recipe[title = "Hard-Boiled Eggs"]
</code>
         <paragraph type="important" sautDePagePdf="0">The dot expression ('.') can be used in predicates and path expressions to refer to the current context node. The following query uses the dot expression to refer to the current <inline>&lt;method&gt;</inline> element. The query selects the empty <inline>&lt;method&gt;</inline> elements from the cookbook.</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')//method[string-length(.) = 0]
</code>
         <paragraph type="important" sautDePagePdf="0">Note that passing the dot expression to the string-<link href="stylesheet-reference.html#length">length()</link> function is optional. When string-<link href="stylesheet-reference.html#length">length()</link> is called with no parameter, the context node is assumed:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')//method[string-length() = 0]
</code>
         <paragraph type="important" sautDePagePdf="0">Actually, selecting an empty <inline>&lt;method&gt;</inline> element might not be very useful by itself. It doesn't tell you which recipe has the empty method:</paragraph>
         <code langage="other">
&lt;method xmlns="http://cookbook/namespace"/&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">What you probably want to see instead are the <inline>&lt;recipe&gt;</inline> elements that have empty <inline>&lt;method&gt;</inline> elements:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')//recipe[string-length(method) = 0]
</code>
         <paragraph type="important" sautDePagePdf="0">The predicate uses the string-<link href="stylesheet-reference.html#length">length()</link> function to test the length of each <inline>&lt;method&gt;</inline> element in each <inline>&lt;recipe&gt;</inline> element found by the node test. If a <inline>&lt;method&gt;</inline> contains no text, the predicate evaluates to <inline>true</inline> and the <inline>&lt;recipe&gt;</inline> element is selected. If the method contains some text, the predicate evaluates to <inline>false</inline>, and the <inline>&lt;recipe&gt;</inline> element is discarded. The output is the entire recipe that has no instructions for preparation:</paragraph>
         <code langage="other">
&lt;recipe xmlns="http://cookbook/namespace" xml:id="HardBoiledEggs"&gt;
    &lt;title&gt;Hard-Boiled Eggs&lt;/title&gt;
    &lt;ingredient name="Eggs" quantity="3" unit="eggs"/&gt;
    &lt;time quantity="3" unit="minutes"/&gt;
    &lt;method/&gt;
&lt;/recipe&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">The astute reader will have noticed that this use of <inline>string-length()</inline> to find an empty element is unreliable. It works in this case, because the method element is written as <inline>&lt;method/&gt;</inline>, guaranteeing that its string length will be 0. It will still work if the method element is written as <inline>&lt;method&gt;&lt;/method&gt;</inline>, but it will fail if there is any whitespace between the opening and ending <inline>&lt;method&gt;</inline> tags. A more robust way to find the recipes with empty methods is presented in the section on <link href="xquery-introduction.html#boolean-predicates">Boolean Predicates</link>.</paragraph>
         <paragraph type="important" sautDePagePdf="0">There are many more functions and operators defined for <link href="xmlprocessing.html">XQuery</link> and XPath. They are all documented in the specification.</paragraph>
      </section>
      <section id="10" noNumber="0" sautDePagePdf="0">
         <title>Positional Predicates</title>
         <paragraph type="important" sautDePagePdf="0">Predicates are often used to filter items based on their position in a sequence. For path expressions processing items loaded from XML documents, the normal sequence is document order. This query returns the second <inline>&lt;recipe&gt;</inline> element in the <inline>cookbook.xml</inline> file:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/recipe[2]
</code>
         <paragraph type="important" sautDePagePdf="0">The other frequently used positional function is last(), which returns the numeric position of the last item in the focus set. Stated another way, last() returns the size of the focus set. This query returns the last recipe in the cookbook:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/recipe[last()]
</code>
         <paragraph type="important" sautDePagePdf="0">And this query returns the next to last <inline>&lt;recipe&gt;</inline>:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/recipe[last() - 1]
</code>
      </section>
      <section id="11" noNumber="0" sautDePagePdf="0">
         <title>Boolean Predicates</title>
         <paragraph type="important" sautDePagePdf="0">The other kind of predicate evaluates to <i>true</i> or <i>false</i>. A boolean predicate takes the value of its expression and determines its <i>effective boolean value</i> according to the following rules:</paragraph>
         <liste sautDePagePdf="0">
            <element useText="0"/>
            <element useText="0"/>
            <element useText="0"/>
            <element useText="0"/>
         </liste>
         <paragraph type="important" sautDePagePdf="0">We have already seen some boolean predicates in use. Earlier, we saw a <i>not so robust</i> way to find the <link href="xquery-introduction.html#empty-method-not-robust">recipes that have no instructions</link>. <inline>[string-length(method) = 0]</inline> is a boolean predicate that would fail in the example if the empty method element was written with both opening and closing tags and there was whitespace between the tags. Here is a more robust way that uses a different boolean predicate.</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/recipe[method[empty(step)]]
</code>
         <paragraph type="important" sautDePagePdf="0">This one uses the empty() and function to test whether the method contains any steps. If the method contains no steps, then <inline>empty(step)</inline> will return <inline>true</inline>, and hence the predicate will evaluate to <inline>true</inline>.</paragraph>
         <paragraph type="important" sautDePagePdf="0">But even that version isn't foolproof. Suppose the method does contain steps, but all the steps themselves are empty. That's still a case of a recipe with no instructions that won't be detected. There is a better way:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/recipe[not(normalize-space(method))]
</code>
         <paragraph type="important" sautDePagePdf="0">This version uses the not and normalize-space() functions. <inline>normalize-space(method))</inline> returns the contents of the method element as a string, but with all the whitespace normalized, i.e., the string value of each <inline>&lt;step&gt;</inline> element will have its whitespace normalized, and then all the normalized step values will be concatenated. If that string is empty, then <inline>not()</inline> returns <inline>true</inline> and the predicate is <inline>true</inline>.</paragraph>
         <paragraph type="important" sautDePagePdf="0">We can also use the position() function in a comparison to inspect positions with conditional logic. The position() function returns the position index of the current context item in the sequence of items:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/recipe[position() = 2]
</code>
         <paragraph type="important" sautDePagePdf="0">Note that the first position in the sequence is position 1, not 0. We can also select <i>all</i> the recipes after the first one:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
doc('cookbook.xml')/cookbook/recipe[position() &gt; 1]
</code>
      </section>
      <section id="12" noNumber="0" sautDePagePdf="0">
         <title>Constructing Elements</title>
         <paragraph type="important" sautDePagePdf="0">In the section about <link href="xquery-introduction.html#wildcards-in-name-tests">using wildcards in name tests</link>, we saw three simple example XQueries, each of which selected a different list of XML attributes from the cookbook. We couldn't use <inline>xmlpatterns</inline> to run these queries, however, because <inline>xmlpatterns</inline> sends the <link href="xmlprocessing.html">XQuery</link> results to a <inline><link href="qxmlserializer.html">serializer</link></inline>, which expects to serialize the results as well-formed XML. Since a list of XML attributes by itself is not well-formed XML, the serializer reported an error for each <link href="xmlprocessing.html">XQuery</link>.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Since an attribute must appear in an element, for each attribute in the result set, we must create an XML element. We can do that using a <i>for</i> clause with a bound variable, and a <i>return</i> clause with an element constructor:</paragraph>
         <code langage="other">
for $i in doc("cookbook.xml")//@xml:*
return &lt;p&gt;{$i}&lt;/p&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">The <i>for</i> clause produces a sequence of attribute nodes from the result of the path expression. Each attribute node in the sequence is bound to the variable <inline>$i</inline>. The <i>return</i> clause then constructs a <inline>&lt;p&gt;</inline> element around the attribute node. Here is the output:</paragraph>
         <code langage="other">
&lt;p xml:id="MushroomSoup"/&gt;
&lt;p xml:id="CheeseOnToast"/&gt;
&lt;p xml:id="HardBoiledEggs"/&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">The output contains one <inline>&lt;p&gt;</inline> element for each <inline>xml:id</inline> attribute in the cookbook. Note that <link href="xmlprocessing.html">XQuery</link> puts each attribute in the right place in its <inline>&lt;p&gt;</inline> element, despite the fact that in the <i>return</i> clause, the <inline>$i</inline> variable is positioned as if it is meant to become <inline>&lt;p&gt;</inline> element content.</paragraph>
         <paragraph type="important" sautDePagePdf="0">The other two examples from the <link href="xquery-introduction.html#wildcards-in-name-tests">wildcard</link> section can be rewritten the same way. Here is the <link href="xmlprocessing.html">XQuery</link> that selects all the <inline>name</inline> attributes, regardless of namespace:</paragraph>
         <code langage="other">
for $i in doc("cookbook.xml")//@*:name
return &lt;p&gt;{$i}&lt;/p&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">And here is its output:</paragraph>
         <code langage="other">
&lt;p name="Fresh mushrooms"/&gt;
&lt;p name="Garlic"/&gt;
&lt;p name="Olive oil"/&gt;
&lt;p name="Milk"/&gt;
&lt;p name="Water"/&gt;
&lt;p name="Cream"/&gt;
&lt;p name="Vegetable soup cube"/&gt;
&lt;p name="Ground black pepper"/&gt;
&lt;p name="Dried parsley"/&gt;
&lt;p name="Bread"/&gt;
&lt;p name="Cheese"/&gt;
&lt;p name="Eggs"/&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">And here is the <link href="xmlprocessing.html">XQuery</link> that selects all the attributes from the <i>document element</i>:</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
for $i in doc("cookbook.xml")/cookbook/@*
return &lt;p&gt;{$i}&lt;/p&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">And here is its output:</paragraph>
         <code langage="other">
&lt;p xmlns="http://cookbook/namespace" count="3"/&gt;
</code>
      </section>
      <section id="13" noNumber="0" sautDePagePdf="0">
         <title>Element Constructors are Expressions</title>
         <paragraph type="important" sautDePagePdf="0">Because node constructors are expressions, they can be used in XQueries wherever expressions are allowed.</paragraph>
         <code langage="other">
declare default element namespace "http://cookbook/namespace";
let $docURI := 'cookbook.xml'
return if(doc-available($docURI))
       then doc($docURI)//recipe/&lt;oppskrift&gt;{./node()}&lt;/oppskrift&gt;
       else &lt;oppskrift&gt;Failed to load {$docURI}&lt;/oppskrift&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">If <inline>cookbook.xml</inline> is loaded without error, a <inline>&lt;oppskrift&gt;</inline> element (Norwegian word for recipe) is constructed for each <inline>&lt;recipe&gt;</inline> element in the cookbook, and the child nodes of the <inline>&lt;recipe&gt;</inline> are copied into the <inline>&lt;oppskrift&gt;</inline> element. But if the cookbook document doesn't exist or does not contain well-formed XML, a single <inline>&lt;oppskrift&gt;</inline> element is constructed containing an error message.</paragraph>
      </section>
      <section id="14" noNumber="0" sautDePagePdf="0">
         <title>Constructing Atomic Values</title>
         <paragraph type="important" sautDePagePdf="0">
            <link href="xmlprocessing.html">XQuery</link> also has atomic values. An atomic value is a value in the value space of one of the built-in datatypes in the XML Schema language. These <i>atomic types</i> have built-in operators for doing arithmetic, comparisons, and for converting values to other atomic types. See the Built-in Datatype Hierarchy for the entire tree of built-in, primitive and derived atomic types.</paragraph>
         <rich-imgtext type="info"/>
         <paragraph type="important" sautDePagePdf="0">To construct an atomic value as element content, enclose an expression in curly braces and embed it in the element constructor:</paragraph>
         <code langage="other">
&lt;e&gt;{sum((1, 2, 3))}&lt;/e&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">Sending this <link href="xmlprocessing.html">XQuery</link> through xmlpatterns produces:</paragraph>
         <code langage="other">
&lt;e&gt;6&lt;/e&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">To compute the value of an attribute, enclose the expression in curly braces and embed it in the attribute value:</paragraph>
         <code langage="other"/>
         <paragraph type="important" sautDePagePdf="0">Sending this <link href="xmlprocessing.html">XQuery</link> through xmlpatterns produces:</paragraph>
         <code langage="other">
&lt;p class="important example obsolete"/&gt;

declare default element namespace "http://cookbook/namespace";
let $docURI := 'cookbook.xml'
return if(doc-available($docURI))
       then doc($docURI)//recipe/&lt;oppskrift&gt;{./node()}&lt;/oppskrift&gt;
       else &lt;oppskrift&gt;Failed to load {$docURI}&lt;/oppskrift&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">If <inline>cookbook.xml</inline> is loaded without error, a <inline>&lt;oppskrift&gt;</inline> element (Norweigian word for recipe) is constructed for each <inline>&lt;recipe&gt;</inline> element in the cookbook, and the child nodes of the <inline>&lt;recipe&gt;</inline> are copied into the <inline>&lt;oppskrift&gt;</inline> element. But if the cookbook document doesn't exist or does not contain well-formed XML, a single <inline>&lt;oppskrift&gt;</inline> element is constructed containing an error message.</paragraph>
      </section>
      <section id="15" noNumber="0" sautDePagePdf="0">
         <title>Running The Cookbook Examples</title>
         <paragraph type="important" sautDePagePdf="0">Most of the <link href="xmlprocessing.html">XQuery</link> examples in this document refer to the <inline>cookbook.xml</inline> example file from the <link href="qtxmlpatterns-recipes-example.html">Recipes Example</link>. Copy the <inline>cookbook.xml</inline> to your current directory, save one of the cookbook <link href="xmlprocessing.html">XQuery</link> examples in a <inline>.xq</inline> file (e.g., <inline>file.xq</inline>), and run the <link href="xmlprocessing.html">XQuery</link> using Qt's command line utility:</paragraph>
         <code langage="other">
xmlpatterns file.xq
</code>
      </section>
      <section id="16" noNumber="0" sautDePagePdf="0">
         <title>Further Reading</title>
         <paragraph type="important" sautDePagePdf="0">There is much more to the <link href="xmlprocessing.html">XQuery</link> language than we have presented in this short introduction. We will be adding more here in later releases. In the meantime, playing with the <inline>xmlpatterns</inline> utility and making modifications to the <link href="xmlprocessing.html">XQuery</link> examples provided here will be quite informative. An <link href="xmlprocessing.html">XQuery</link> textbook will be a good investment.</paragraph>
         <paragraph type="important" sautDePagePdf="0">You can also ask questions on <link href="xmlprocessing.html">XQuery</link> mail lists:</paragraph>
         <liste sautDePagePdf="0">
            <element useText="0"/>
            <element useText="0"/>
         </liste>
         <paragraph type="important" sautDePagePdf="0">FunctX has a collection of <link href="xmlprocessing.html">XQuery</link> functions that can be both useful and educational.</paragraph>
         <paragraph type="important" sautDePagePdf="0">This introduction contains many links to the specifications, which, of course, are the ultimate source of information about <link href="xmlprocessing.html">XQuery</link>. They can be a bit difficult, though, so consider investing in a textbook:</paragraph>
         <liste sautDePagePdf="0">
            <element useText="0"/>
            <element useText="0"/>
         </liste>
      </section>
      <section id="17" noNumber="0" sautDePagePdf="0">
         <title>FAQ</title>
         <paragraph type="important" sautDePagePdf="0">The answers to these frequently asked questions explain the causes of several common mistakes that most beginners make. Reading through the answers ahead of time might save you a lot of head scratching.</paragraph>
      </section>
      <section id="18" noNumber="0" sautDePagePdf="0">
         <title>Why didn't my path expression match anything?</title>
         <paragraph type="important" sautDePagePdf="0">The most common cause of this bug is failure to declare one or more namespaces in your <link href="xmlprocessing.html">XQuery</link>. Consider the following query for selecting all the examples in an XHTML document:</paragraph>
         <code langage="other">doc("index.html")/html/body/p[@class="example"]
</code>
         <paragraph type="important" sautDePagePdf="0">It won't match anything because <inline>index.html</inline> is an XHTML file, and all XHTML files declare the default namespace <inline>"http://www.w3.org/1999/xhtml"</inline> in their top (<inline>&lt;html&gt;</inline>) element. But the query doesn't declare this namespace, so the path expression expands <inline>html</inline> to <inline>{}html</inline> and tries to match that expanded name. But the actual expanded name is <inline>{http://www.w3.org/1999/xhtml}html</inline>. One possible fix is to declare the correct default namespace in the <link href="xmlprocessing.html">XQuery</link>:</paragraph>
         <code langage="other">declare namespace x = "http://www.w3.org/1999/xhtml/";
doc("index.html")/x:html/x:body/x:p[@class="example"]
</code>
         <paragraph type="important" sautDePagePdf="0">Another common cause of this bug is to confuse the <i>document node</i> with the top element node. They are different. This query won't match anything:</paragraph>
         <code langage="other">doc("myPlainHTML.html")/body
</code>
         <paragraph type="important" sautDePagePdf="0">The <inline>doc()</inline> function returns the <i>document node</i>, not the top element node (<inline>&lt;html&gt;</inline>). Don't forget to match the top element node in the path expression:</paragraph>
         <code langage="other">doc("myPlainHTML.html")/html/body
</code>
      </section>
      <section id="19" noNumber="0" sautDePagePdf="0">
         <title>What if my input namespace is different from my output namespace?</title>
         <paragraph type="important" sautDePagePdf="0">Just remember to declare both namespaces in your <link href="xmlprocessing.html">XQuery</link> and use them properly. Consider the following query, which is meant to generate XHTML output from XML input:</paragraph>
         <code langage="other">declare default element namespace "http://www.w3.org/1999/xhtml";
&lt;html&gt;
    &lt;body&gt;
        {
            for $i in doc("testResult.xml")/tests/test[@status = "failure"]
            order by $i/@name
            return &lt;p&gt;{$i/@name}&lt;/p&gt;
        }
    &lt;/body&gt;
&lt;/html&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">We want the <inline>&lt;html&gt;</inline>, <inline>&lt;body&gt;</inline>, and <inline>&lt;p&gt;</inline> nodes we create in the output to be in the standard XHTML namespace, so we declare the default namespace to be <inline>http://www.w3.org/1999/xhtml</inline>. That's correct for the output, but that same default namespace will also be applied to the node names in the path expression we're trying to match in the input (<inline>/tests/test[@status = "failure"]</inline>), which is wrong, because the namespace used in <inline>testResult.xml</inline> is perhaps in the empty namespace. So we must declare that namespace too, with a namespace prefix, and then use the prefix with the node names in the path expression. This one will probably work better:</paragraph>
         <code langage="other">declare namespace x = "http://www.w3.org/1998/xhtml";
&lt;x:html&gt;
    &lt;x:body&gt;
        {
            for $i in doc("testResult.xml")/tests/test[@status = "failure"]
            order by $i/@name
            return &lt;x:p&gt;{$i/@name}&lt;/x:p&gt;
        }
    &lt;/x:body&gt;
&lt;/x:html&gt;
</code>
      </section>
      <section id="20" noNumber="0" sautDePagePdf="0">
         <title>Why doesn't my return clause work?</title>
         <paragraph type="important" sautDePagePdf="0">Recall that <link href="xmlprocessing.html">XQuery</link> is an <i>expression-based</i> language, not <i>statement-based</i>. Because an <link href="xmlprocessing.html">XQuery</link> is a lot of expressions, understanding <link href="xmlprocessing.html">XQuery</link> expression precedence is very important. Consider the following query:</paragraph>
         <code langage="other">for $i in(reverse(1 to 10)),
    $d in xs:integer(doc("numbers.xml")/numbers/number)
return $i + $d
</code>
         <paragraph type="important" sautDePagePdf="0">It looks ok, but it isn't. It is supposed to be a FLWOR expression comprising a <i>for</i> clause and a <i>return</i> clause, but it isn't just that. It <i>has</i> a FLWOR expression, certainly (with the <i>for</i> and <i>return</i> clauses), but it <i>also</i> has an arithmetic expression (<i>+ $d</i>) dangling at the end because we didn't enclose the return expression in parentheses.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Using parentheses to establish precedence is more important in <link href="xmlprocessing.html">XQuery</link> than in other languages, because <link href="xmlprocessing.html">XQuery</link> is <i>expression-based</i>. In In this case, without parantheses enclosing <inline>$i + $d</inline>, the return clause only returns <inline>$i</inline>. The <inline>+$d</inline> will have the result of the FLWOR expression as its left operand. And, since the scope of variable <inline>$d</inline> ends at the end of the <i>return</i> clause, a variable out of scope error will be reported. Correct these problems by using parentheses.</paragraph>
         <code langage="other">for $i in(reverse(1 to 10)),
    $d in xs:integer(doc("numbers.xml")/numbers/number)
return ($i + $d)
</code>
      </section>
      <section id="21" noNumber="0" sautDePagePdf="0">
         <title>Why didn't my expression get evaluated?</title>
         <paragraph type="important" sautDePagePdf="0">You probably misplaced some curly braces. When you want an expression evaluated inside an element constructor, enclose the expression in curly braces. Without the curly braces, the expression will be interpreted as text. Here is a <inline>sum()</inline> expression used in an <inline>&lt;e&gt;</inline> element. The table shows cases where the curly braces are missing, misplaced, and placed correctly:</paragraph>
         <tableau width="80%" border="1" sautDePagePdf="0">
            <entete>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </entete>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
            <ligne>
               <colonne useText="0"/>
               <colonne useText="0"/>
            </ligne>
         </tableau>
      </section>
      <section id="22" noNumber="0" sautDePagePdf="0">
         <title>My predicate is correct, so why doesn't it select the right stuff?</title>
         <paragraph type="important" sautDePagePdf="0">Either you put your predicate in the wrong place in your path expression, or you forgot to add some parentheses. Consider this input file <inline>doc.txt</inline>:</paragraph>
         <code langage="other">&lt;doc&gt;
  &lt;p&gt;
    &lt;span&gt;1&lt;/span&gt;
    &lt;span&gt;2&lt;/span&gt;
  &lt;/p&gt;
  &lt;p&gt;
    &lt;span&gt;3&lt;/span&gt;
    &lt;span&gt;4&lt;/span&gt;
  &lt;/p&gt;
  &lt;p&gt;
    &lt;span&gt;5&lt;/span&gt;
    &lt;span&gt;6&lt;/span&gt;
  &lt;/p&gt;
  &lt;p&gt;
    &lt;span&gt;7&lt;/span&gt;
    &lt;span&gt;8&lt;/span&gt;
  &lt;/p&gt;
  &lt;p&gt;
    &lt;span&gt;9&lt;/span&gt;
    &lt;span&gt;a&lt;/span&gt;
  &lt;/p&gt;
  &lt;p&gt;
    &lt;span&gt;b&lt;/span&gt;
    &lt;span&gt;c&lt;/span&gt;
  &lt;/p&gt;
  &lt;p&gt;
    &lt;span&gt;d&lt;/span&gt;
    &lt;span&gt;e&lt;/span&gt;
  &lt;/p&gt;
  &lt;p&gt;
    &lt;span&gt;f&lt;/span&gt;
    &lt;span&gt;0&lt;/span&gt;
  &lt;/p&gt;
&lt;/doc&gt;

</code>
         <paragraph type="important" sautDePagePdf="0">Suppose you want the first <inline>&lt;span&gt;</inline> element of every <inline>&lt;p&gt;</inline> element. Apply a position filter (<inline>[1]</inline>) to the <inline>/span</inline> path step:</paragraph>
         <code langage="other">let $doc := doc('doc.txt')
return $doc/doc/p/span[1]</code>
         <paragraph type="important" sautDePagePdf="0">Applying the <inline>[1]</inline> filter to the <inline>/span</inline> step returns the first <inline>&lt;span&gt;</inline> element of each <inline>&lt;p&gt;</inline> element:</paragraph>
         <code langage="other">
&lt;span&gt;1&lt;/span&gt;
&lt;span&gt;3&lt;/span&gt;
&lt;span&gt;5&lt;/span&gt;
&lt;span&gt;7&lt;/span&gt;
&lt;span&gt;9&lt;/span&gt;
&lt;span&gt;b&lt;/span&gt;
&lt;span&gt;d&lt;/span&gt;
&lt;span&gt;f&lt;/span&gt;
</code>
         <rich-imgtext type="info"/>
         <code langage="other">
for $a in doc('doc.txt')/doc/p/span[1]
return $a
</code>
         <paragraph type="important" sautDePagePdf="0">Or you can reduce it right down to this:</paragraph>
         <code langage="other">
doc('doc.txt')/doc/p/span[1]
</code>
         <paragraph type="important" sautDePagePdf="0">On the other hand, suppose you really want only one <inline>&lt;span&gt;</inline> element, the first one in the document (i.e., you only want the first <inline>&lt;span&gt;</inline> element in the first <inline>&lt;p&gt;</inline> element). Then you have to do more filtering. There are two ways you can do it. You can apply the <inline>[1]</inline> filter in the same place as above but enclose the path expression in parentheses:</paragraph>
         <code langage="other">let $doc := doc('doc.txt')
return ($doc/doc/p/span)[1]
</code>
         <paragraph type="important" sautDePagePdf="0">Or you can apply a second position filter (<inline>[1]</inline> again) to the <inline>/p</inline> path step:</paragraph>
         <code langage="other">
let $doc := doc('doc.txt')
return $doc/doc/p[1]/span[1]
</code>
         <paragraph type="important" sautDePagePdf="0">Either way the query will return only the first <inline>&lt;span&gt;</inline> element in the document:</paragraph>
         <code langage="other">
&lt;span&gt;1&lt;/span&gt;
</code>
      </section>
      <section id="23" noNumber="0" sautDePagePdf="0">
         <title>Why doesn't my FLWOR behave as expected?</title>
         <paragraph type="important" sautDePagePdf="0">The quick answer is you probably expected your <link href="xmlprocessing.html">XQuery</link> FLWOR to behave just like a C++ <i>for</i> loop. But they aren't the same. Consider a simple example:</paragraph>
         <code langage="other">for $a in (8, -4, 2)
let $b := ($a * -1, $a)
order by $a
return $b
</code>
         <paragraph type="important" sautDePagePdf="0">This query evaluates to <i>4 -4 -2 2 -8 8</i>. The <i>for</i> clause does set up a <i>for</i> loop style iteration, which does evaluate the rest of the FLWOR multiple times, one time for each value returned by the <i>in</i> expression. That much is similar to the C++ <i>for</i> loop.</paragraph>
         <paragraph type="important" sautDePagePdf="0">But consider the <i>return</i> clause. In C++ if you hit a <i>return</i> statement, you break out of the <i>for</i> loop and return from the function with one value. Not so in <link href="xmlprocessing.html">XQuery</link>. The <i>return</i> clause is the last clause of the FLWOR, and it means: <i>Append the return value to the result list and then begin the next iteration of the FLWOR</i>. When the <i>for</i> clause's <i>in</i> expression no longer returns a value, the entire result list is returned.</paragraph>
         <paragraph type="important" sautDePagePdf="0">Next, consider the <i>order by</i> clause. It doesn't do any sorting on each iteration of the FLWOR. It just evaluates its expression on each iteration (<inline>$a</inline> in this case) to get an ordering value to map to the result item from each iteration. These ordering values are kept in a parallel list. The result list is sorted at the end using the parallel list of ordering values.</paragraph>
         <paragraph type="important" sautDePagePdf="0">The last difference to note here is that the <i>let</i> clause does <i>not</i> set up an iteration through a sequence of values like the <i>for</i> clause does. The <i>let</i> clause isn't a sort of nested loop. It isn't a loop at all. It is just a variable binding. On each iteration, it binds the <i>entire</i> sequence of values on the right to the variable on the left. In the example above, it binds (4 -4) to <inline>$b</inline> on the first iteration, (-2 2) on the second iteration, and (-8 8) on the third iteration. So the following query doesn't iterate through anything, and doesn't do any ordering:</paragraph>
         <code langage="other">let $i := (2, 3, 1)
order by $i[1]
return $i
</code>
         <paragraph type="important" sautDePagePdf="0">It binds the entire sequence (2, 3, 1) to <inline>$i</inline> one time only; the <i>order by</i> clause only has one thing to order and hence does nothing, and the query evaluates to 2 3 1, the sequence assigned to <inline>$i</inline>.</paragraph>
         <rich-imgtext type="info"/>
      </section>
      <section id="24" noNumber="0" sautDePagePdf="0">
         <title>Why are my elements created in the wrong order?</title>
         <paragraph type="important" sautDePagePdf="0">The short answer is your elements are <i>not</i> created in the wrong order, because when appearing as operands to a path expression, there is no correct order. Consider the following query, which again uses the input file <inline>doc.txt</inline>:</paragraph>
         <code langage="other">
doc('doc.txt')//p/&lt;p&gt;{span/node()}&lt;/p&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">The query finds all the <inline>&lt;p&gt;</inline> elements in the file. For each <inline>&lt;p&gt;</inline> element, it builds a <inline>&lt;p&gt;</inline> element in the output containing the concatenated contents of all the <inline>&lt;p&gt;</inline> element's child <inline>&lt;span&gt;</inline> elements. Running the query through <inline>xmlpatterns</inline> might produce the following output, which is not sorted in the expected order.</paragraph>
         <code langage="other">
&lt;p&gt;78&lt;/p&gt;
&lt;p&gt;9a&lt;/p&gt;
&lt;p&gt;12&lt;/p&gt;
&lt;p&gt;bc&lt;/p&gt;
&lt;p&gt;de&lt;/p&gt;
&lt;p&gt;34&lt;/p&gt;
&lt;p&gt;56&lt;/p&gt;
&lt;p&gt;f0&lt;/p&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">You can use a <i>for</i> loop to ensure that the order of the result set corresponds to the order of the input sequence:</paragraph>
         <code langage="other">
for $a in doc('doc.txt')//p
 return &lt;p&gt;{$a/span/node()}&lt;/p&gt;
</code>
         <paragraph type="important" sautDePagePdf="0">This version produces the same result set but in the expected order:</paragraph>
         <code langage="other">
&lt;p&gt;12&lt;/p&gt;
&lt;p&gt;34&lt;/p&gt;
&lt;p&gt;56&lt;/p&gt;
&lt;p&gt;78&lt;/p&gt;
&lt;p&gt;9a&lt;/p&gt;
&lt;p&gt;bc&lt;/p&gt;
&lt;p&gt;de&lt;/p&gt;
&lt;p&gt;f0&lt;/p&gt;
</code>
      </section>
      <section id="25" noNumber="0" sautDePagePdf="0">
         <title>Why can't I use true and false in my XQuery?</title>
         <paragraph type="important" sautDePagePdf="0">You can, but not by just using the names <inline>true</inline> and <inline>false</inline> directly, because they are <link href="xquery-introduction.html#name-tests">name tests</link> although they look like boolean constants. The simple way to create the boolean values is to use the builtin functions <inline>true()</inline> and <inline>false()</inline> wherever you want to use <inline>true</inline> and <inline>false</inline>. The other way is to invoke the boolean constructor:</paragraph>
         <code langage="other">xs:boolean("true")
</code>
      </section>
   </summary>
</document>
